STATIC-LINKING SYMBOL COLLISION -- EVIDENCE SUMMARY
generated by evidence.sh on aarch64 / aarch64-linux-gnu

SCENARIO 1: cross-library interposition (4 archives, task link line)
  -lcollective_a -lverbs_vendor -lcollective_b -lverbs_bundled
  collision       : 1 wrong-device line(s)  -> SILENT misdirection, no link error
  link-order      : 1 wrong-device line(s)  -> reversing order moves the victim
  explicit-error  : 2 'multiple definition' -> forcing both members = LOUD error
  fixed-groups    : 1 wrong-device line(s)  -> --start-group does NOT fix it
  fixed-visibility: 1 wrong-device line(s)  -> -fvisibility=hidden does NOT fix it
  fixed-objcopy   : localize=1 wrong-device line(s) total (exp1 fails, exp2 redefine-sym FIXES)

SCENARIO 2: vendored copy, def+use co-located (ld -r merged objects)
  vendored-collision       : 4 'multiple definition' -> LOUD, not silent
  vendored-fixed-localize  : 0 wrong-device -> objcopy --localize-symbol FIXES
  vendored-fixed-visibility: 0 wrong-device -> hidden + --localize-hidden FIXES

VERDICT
  The silent misdirection is first-definition-wins across duplicate strong
  symbols, made silent by one-symbol-per-member archive granularity.
  Fixes that do NOT work for the cross-library layout: --start-group,
  -fvisibility=hidden, objcopy --localize-symbol (member never pulled).
  Fixes that DO work: namespacing (objcopy --redefine-sym / rename),
  a single canonical copy, or vendoring correctly (merge + localize so
  each library privately owns its copy).
